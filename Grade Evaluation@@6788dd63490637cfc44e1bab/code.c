#include <stdio.h>


int main() {
    char grades;
    scanf("%c",&grades);

    if(grade=='A'){
        ptintf("Excellent");
    }
    if(grade=='B'){
        ptintf("Good");
    }
    if(grade=='C'){
        ptintf("Average");
    }
    if(grade=='D'){
        ptintf("Below Average");
    }
    if(grade=='F'){
        ptintf("Fail");
    }
    
    return 0;
}